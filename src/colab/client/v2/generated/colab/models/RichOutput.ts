// @ts-nocheck
/* tslint:disable */
/* eslint-disable */
/**
 * Colab API
 * The Colab API lets you programmatically manage Colab runtimes.  The API is currently in beta and available on an allowlist basis. If you\'re interested in integrating with us, please share your use cases with colaboratory-team@google.com to request access. We look forward to working with you!
 *
 * The version of the OpenAPI document: v1beta
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { ErrorInfo } from './ErrorInfo';
import {
    ErrorInfoFromJSON,
    ErrorInfoFromJSONTyped,
    ErrorInfoToJSON,
    ErrorInfoToJSONTyped,
} from './ErrorInfo';

/**
 * A single rich (non-text) output the runtime captured for this execution.
 * @export
 * @interface RichOutput
 */
export interface RichOutput {
    /**
     * Why the output was not written, set iff it was dropped.
     * @type {ErrorInfo}
     * @memberof RichOutput
     */
    errorInfo?: ErrorInfo;
    /**
     * Absolute `/content/...` path to the nbformat bundle written on the
     * runtime's filesystem, e.g.
     * `/content/.dsm_outputs/{execution_msg_id}/out-000.json`. Set iff the
     * output was written. Keys only, never bytes; the bytes can be fetched
     * from the live runtime out of band.
     * @type {string}
     * @memberof RichOutput
     */
    filePath?: string;
    /**
     * 0-based index of this output among the execution's rich outputs, in
     * emission order. Dense and monotonic: a dropped output still occupies its
     * index, so later indices never shift. This is the stable identifier for a
     * dropped output, which has no `file_path`.
     * @type {number}
     * @memberof RichOutput
     */
    index?: number;
    /**
     * The rich MIME representations this output carries, in the kernel's
     * emission order, e.g. ["image/png", "text/html"]. These are alternative
     * renderings of the SAME output; a client picks the one it can render and
     * reads it from the bundle at `file_path` (i.e. data[mime]). Excludes the
     * always-present "text/plain" fallback (still present in the bundle) and
     * Colab's internal intrinsic type. Non-empty even for a dropped output
     * (whose bundle was never written), so a client can tell what was lost.
     * @type {Array<string>}
     * @memberof RichOutput
     */
    mimeTypes?: Array<string>;
    /**
     * Size in bytes of the nbformat bundle file at `file_path` as written on
     * the runtime's disk. This is the base64-inflated on-disk bundle size (i.e.
     * how many bytes a fetch transfers), NOT the decoded asset size. A hint so
     * a client can decide whether to fetch. Set only for a written output; 0
     * (omitted) for a dropped one.
     * @type {string}
     * @memberof RichOutput
     */
    sizeBytes?: string;
}

/**
 * Check if a given object implements the RichOutput interface.
 */
export function instanceOfRichOutput(value: object): value is RichOutput {
    return true;
}

export function RichOutputFromJSON(json: any): RichOutput {
    return RichOutputFromJSONTyped(json, false);
}

export function RichOutputFromJSONTyped(json: any, ignoreDiscriminator: boolean): RichOutput {
    if (json == null) {
        return json;
    }
    return {
        
        'errorInfo': json['errorInfo'] == null ? undefined : ErrorInfoFromJSON(json['errorInfo']),
        'filePath': json['filePath'] == null ? undefined : json['filePath'],
        'index': json['index'] == null ? undefined : json['index'],
        'mimeTypes': json['mimeTypes'] == null ? undefined : json['mimeTypes'],
        'sizeBytes': json['sizeBytes'] == null ? undefined : json['sizeBytes'],
    };
}

export function RichOutputToJSON(json: any): RichOutput {
    return RichOutputToJSONTyped(json, false);
}

export function RichOutputToJSONTyped(value?: RichOutput | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'errorInfo': ErrorInfoToJSON(value['errorInfo']),
        'filePath': value['filePath'],
        'index': value['index'],
        'mimeTypes': value['mimeTypes'],
        'sizeBytes': value['sizeBytes'],
    };
}

